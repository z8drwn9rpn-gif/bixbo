import { Link } from "@tanstack/react-router";
import { useMemo, useRef, useState, type CSSProperties } from "react";
import { AppShell } from "@/components/AppShell";
import { ArrowLeft } from "@/components/icons/BixboIcons";
import { EMPTY, useBixbo, type DayLog, type Med } from "@/lib/storage";
import { useI18n } from "@/hooks/useI18n";

type Preset = "7" | "30" | "90" | "365" | "custom";
type PickerTarget = "from" | "to";
type MedLog = Record<string, Record<string, boolean>>;
type RDay = { key:string; log:DayLog; pain?:number; head?:number; flash?:number; nausea?:number; tetany?:number; panic?:number; bowel?:number; sleep?:number; notes:string[] };

const iso=(d:Date)=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`;
const fromIso=(s:string)=>{const[y,m,d]=s.split("-").map(Number);return new Date(y,m-1,d)};
const add=(s:string,n:number)=>{const d=fromIso(s);d.setDate(d.getDate()+n);return iso(d)};
const countDays=(a:string,b:string)=>Math.max(1,Math.round((+fromIso(b)-+fromIso(a))/86400000)+1);
const avg=(a:number[])=>a.length?a.reduce((x,y)=>x+y,0)/a.length:undefined;
const mx=(a:number[])=>a.length?Math.max(...a):undefined;
const fmt=(x:number|undefined)=>x==null||!Number.isFinite(x)?"—":x.toFixed(1);
const pct=(a:number,b:number)=>b?Math.round(a/b*100):0;
const longDate=(k:string,l:string)=>new Intl.DateTimeFormat(l,{day:"2-digit",month:"short",year:"numeric"}).format(fromIso(k));
const shortDate=(k:string,l:string)=>new Intl.DateTimeFormat(l,{day:"2-digit",month:"short"}).format(fromIso(k));
const monthLabel=(k:string,l:string)=>new Intl.DateTimeFormat(l,{month:"short"}).format(fromIso(k));

function summarize(key:string,log:DayLog,raw:unknown):RDay{const p=log.pain??[];const notes=Array.isArray(raw)?raw.map(x=>typeof x==="string"?x:typeof x==="object"&&x&&"text" in x?String((x as {text?:unknown}).text??""):"").filter(Boolean):[];const scores=p.map(x=>Number(x.score)).filter(Number.isFinite);return{key,log,notes,pain:avg(scores),head:mx(p.map(x=>x.headacheIntensity).filter((x):x is number=>typeof x==="number")),flash:mx(p.map(x=>x.hotFlashes).filter((x):x is number=>typeof x==="number")),nausea:mx(p.map(x=>x.nauseaSeverity).filter((x):x is number=>typeof x==="number")),tetany:mx((log.tetany??[]).map(x=>x.intensity)),panic:mx((log.panic??[]).map(x=>x.intensity)),bowel:log.bowel?.at(-1)?.bristol,sleep:log.sleepHours}}
function facts(d:RDay){const a:string[]=[];if(d.pain!=null)a.push(`Pain ${fmt(d.pain)}/10`);if(d.head!=null)a.push(`Headache ${fmt(d.head)}/10`);if(d.flash!=null)a.push(`Hot flash ${fmt(d.flash)}/5`);if(d.tetany!=null)a.push(`Tetany ${fmt(d.tetany)}/5`);if(d.panic!=null)a.push(`Panic ${fmt(d.panic)}/5`);if(d.nausea!=null)a.push(`Nausea ${fmt(d.nausea)}/5`);if(d.bowel!=null)a.push(`Bowel Type ${d.bowel}`);return a}
function meaningful(d:RDay){const l=d.log;return Boolean(facts(d).length||d.notes.length||l.heat?.length||l.food?.length||l.sex?.length||l.extraMeds?.length||l.workout?.length||l.period||l.periodInfo?.level||l.sleepHours!=null||l.temperature!=null||l.temperatureEntries?.length)}
function Header({range,page}:{range:string;page:number}){return <div className="hrHeader"><div><b>BIXBO</b>{page===1?<h1>Health Report</h1>:<h3>Health Report</h3>}</div><span>{range}</span></div>}
function Foot({page,total}:{page:number;total:number}){return <footer><span>BIXBO Health Report · user-recorded health data</span><span>Page {page} of {total}</span></footer>}
function Metric({label,value,hint}:{label:string;value:string;hint?:string}){return <div className="metric"><span>{label}</span><strong>{value}</strong>{hint&&<small>{hint}</small>}</div>}

function Heatmap({days}:{days:RDay[]}){const buckets=days.length<=31?days.map(d=>({key:d.key,label:String(fromIso(d.key).getDate()),days:[d]})):(()=>{const m=new Map<string,RDay[]>();days.forEach(d=>{const x=fromIso(d.key),k=`${x.getFullYear()}-${String(x.getMonth()+1).padStart(2,"0")}`;m.set(k,[...(m.get(k)??[]),d])});return[...m].slice(-12).map(([k,ds])=>({key:k,label:monthLabel(`${k}-01`,"en-GB"),days:ds}))})();const rows:[string,(d:RDay)=>number|undefined,number][]=[["Period / spotting",d=>d.log.periodInfo?.level||d.log.period?1:undefined,1],["Pain",d=>d.pain,10],["Headache",d=>d.head,10],["Hot flashes",d=>d.flash,5],["Tetany",d=>d.tetany,5],["Panic attack",d=>d.panic,5],["Nausea",d=>d.nausea,5]];return <div className="heat" style={{"--cells":buckets.length} as CSSProperties}><div className="heatRow heatHead"><span/>{buckets.map(b=><b key={b.key}>{b.label}</b>)}</div>{rows.map(([name,get,scale],ri)=><div className="heatRow" key={name}><span>{name}</span>{buckets.map(b=>{const v=avg(b.days.map(get).filter((x):x is number=>x!=null));const level=v==null?0:Math.max(1,Math.min(4,Math.ceil(v/scale*4)));return <i key={b.key} data-l={level} data-period={ri===0&&v!=null?"true":undefined}/>})}</div>)}</div>}

function DailyPainTrend({days,locale}:{days:RDay[];locale:string}){const pts=days.map((d,i)=>d.pain==null?null:{i,v:d.pain,key:d.key}).filter((x):x is {i:number;v:number;key:string}=>!!x);if(pts.length<2)return <div className="empty">Not enough pain data for a trend.</div>;const w=650,h=175,L=34,R=10,T=18,B=35,x=(i:number)=>L+i/Math.max(1,days.length-1)*(w-L-R),y=(v:number)=>h-B-v/10*(h-T-B),tick=Math.max(1,Math.ceil(days.length/10));const segs:{i:number;v:number;key:string}[][]=[];let seg:{i:number;v:number;key:string}[]=[];pts.forEach((p,i)=>{if(i&&p.i!==pts[i-1].i+1){if(seg.length)segs.push(seg);seg=[]}seg.push(p)});if(seg.length)segs.push(seg);return <><svg viewBox={`0 0 ${w} ${h}`} className="chart">{[0,2,4,6,8,10].map(v=><g key={v}><line x1={L} x2={w-R} y1={y(v)} y2={y(v)}/><text x={L-7} y={y(v)+3} textAnchor="end">{v}</text></g>)}{days.map((d,i)=>(i%tick===0||i===days.length-1)?<text key={d.key} className="xLabel" x={x(i)} y={h-10} textAnchor="middle">{shortDate(d.key,locale)}</text>:null)}{segs.map((s,i)=>s.length>1?<polyline key={i} points={s.map(p=>`${x(p.i)},${y(p.v)}`).join(" ")}/>:null)}{pts.map(p=><g key={p.key}><circle cx={x(p.i)} cy={y(p.v)} r="3"/><text className="pointLabel" x={x(p.i)} y={y(p.v)-7} textAnchor="middle">{fmt(p.v)}</text></g>)}</svg><div className="trendLegend"><span><i/>Pain (daily average)</span><span>Missing day = no recorded pain value, not zero</span></div></>}

type BarItem={key:string;label:string;value?:number};
function PainBarTrend({days,locale}:{days:RDay[];locale:string}){let title="Daily average",items:BarItem[]=[];if(days.length<=7){items=days.map(d=>({key:d.key,label:shortDate(d.key,locale),value:d.pain}))}else if(days.length<=45){title="Weekly average";for(let i=0;i<days.length;i+=7){const ds=days.slice(i,i+7);items.push({key:ds[0].key,label:`${fromIso(ds[0].key).getDate()}–${fromIso(ds.at(-1)!.key).getDate()} ${monthLabel(ds.at(-1)!.key,locale)}`,value:avg(ds.map(d=>d.pain).filter((x):x is number=>x!=null))})}}else{title="Monthly average";const g=new Map<string,RDay[]>();days.forEach(d=>{const x=fromIso(d.key),k=`${x.getFullYear()}-${String(x.getMonth()+1).padStart(2,"0")}`;g.set(k,[...(g.get(k)??[]),d])});items=[...g].map(([k,ds])=>({key:k,label:monthLabel(`${k}-01`,locale),value:avg(ds.map(d=>d.pain).filter((x):x is number=>x!=null))})).slice(days.length>=300?-12:-6)}return <div><div className="barTitle">Pain trend <small>(0–10) · {title.toLowerCase()}</small></div><div className="painBars" style={{"--bars":items.length} as CSSProperties}>{items.map(i=><div className="painBar" key={i.key}><div className="painBarArea">{i.value!=null?<><b style={{bottom:`calc(${Math.max(3,i.value*10)}% + 3px)`}}>{i.value.toFixed(1)}</b><i style={{height:`${Math.max(3,i.value*10)}%`}}/></>:<em>—</em>}</div><span>{i.label}</span></div>)}</div><div className="miniLegend"><i/> {title} pain <span className="nodata">▧ No data</span></div></div>}

function slotKeys(m:Med){return(m.times??[]).map(t=>`${m.id}@${t}`)}
function doseState(day:Record<string,boolean>|undefined,m:Med){const slotValues=slotKeys(m).map(k=>day?.[k]).filter((v):v is boolean=>typeof v==="boolean");if(slotValues.length)return{tracked:slotValues.length,taken:slotValues.filter(Boolean).length};if(typeof day?.[m.id]==="boolean")return{tracked:1,taken:day[m.id]?1:0};return{tracked:0,taken:0}}
function adherenceForMed(m:Med,days:RDay[],medLog:MedLog){let taken=0,tracked=0;for(const d of days){const s=doseState(medLog[d.key],m);taken+=s.taken;tracked+=s.tracked}return tracked?{taken,total:tracked,pct:pct(taken,tracked)}:null}
function takenText(d:RDay,meds:Med[],medLog:MedLog){const out:string[]=[];for(const m of meds.filter(x=>!x.asNeeded)){const s=doseState(medLog[d.key],m);if(s.taken)out.push(s.taken>1?`${m.name} ${s.taken}x`:m.name)}return out.join(", ")||"—"}
function extraText(d:RDay){return(d.log.extraMeds??[]).map(x=>`${x.name}${x.dose?` ${x.dose}`:""}`).join(", ")||"—"}
function tensText(d:RDay){const a=(d.log.heat??[]).filter(x=>x.kind==="tens");return a.length?a.map(x=>x.minutes?`${x.minutes} min`:"TENS").join(", "):"—"}
function contextText(d:RDay){const a:string[]=[];if(d.log.workout?.length)a.push(`Workout: ${d.log.workout.map(x=>`${x.kind}${x.minutes?` ${x.minutes} min`:""}`).join(", ")}`);if(d.sleep!=null)a.push(`Sleep ${fmt(d.sleep)} h`);const ts=d.log.temperatureEntries?.map(x=>x.value)??[];if(ts.length)a.push(`Temperature ${ts.map(v=>`${v}°C`).join(", ")}`);else if(d.log.temperature!=null)a.push(`Temperature ${d.log.temperature}°C`);if(d.log.food?.length)a.push(`Food logged (${d.log.food.length})`);if(d.notes.length)a.push(d.notes.join(" · "));return a.join(" · ")||"—"}
function packTimeline(rows:RDay[],meds:Med[],medLog:MedLog){const pages:RDay[][]=[];let page:RDay[]=[];let budget=0;for(const d of rows){const text=[facts(d).join(" "),takenText(d,meds,medLog),extraText(d),tensText(d),contextText(d)].join(" ");const cost=Math.max(1,Math.ceil(text.length/170));if(page.length&&(budget+cost>30||page.length>=19)){pages.push(page);page=[];budget=0}page.push(d);budget+=cost}if(page.length)pages.push(page);if(!pages.length)pages.push([]);return pages}

const PAIN_SCALE=[["0","No pain"],["1–2","Very mild"],["3–4","Mild"],["5–6","Moderate"],["7–8","Severe"],["9–10","Very severe"]] as const;
const BOWEL=[[0,"No bowel movement / constipation"],[1,"Separate hard lumps, like nuts"],[2,"Sausage-shaped but lumpy"],[3,"Like a sausage but with cracks on the surface"],[4,"Like a sausage or snake, smooth and soft"],[5,"Soft blobs with clear-cut edges"],[6,"Fluffy pieces with ragged edges"],[7,"Watery, no solid pieces"]] as const;

function Report({days,meds,medLog,range,locale}:{days:RDay[];meds:Med[];medLog:MedLog;range:string;locale:string}){const logged=days.filter(meaningful),pain=days.filter(d=>d.pain!=null),head=days.filter(d=>d.head!=null),flash=days.filter(d=>d.flash!=null),tet=days.filter(d=>d.tetany!=null),panic=days.filter(d=>d.panic!=null),nausea=days.filter(d=>d.nausea!=null),sleep=days.filter(d=>d.sleep!=null),bowel=days.filter(d=>d.bowel!=null);const pAvg=avg(pain.map(d=>d.pain!)),pMax=mx(pain.map(d=>d.pain!)),pMin=pain.length?Math.min(...pain.map(d=>d.pain!)):undefined,hAvg=avg(head.map(d=>d.head!)),hMax=mx(head.map(d=>d.head!)),fAvg=avg(flash.map(d=>d.flash!)),fMax=mx(flash.map(d=>d.flash!)),sAvg=avg(sleep.map(d=>d.sleep!)),common=bowel.length?Object.entries(bowel.reduce((m,d)=>{m[d.bowel!]=(m[d.bowel!]??0)+1;return m},{} as Record<number,number>)).sort((a,b)=>b[1]-a[1])[0]?.[0]:undefined;const symptoms:[string,RDay[]][]=[["Pain",pain],["Headache",head],["Hot flashes",flash],["Tetany",tet],["Panic attack",panic],["Nausea",nausea]];const patterns:string[]=[];if(head.length>=2)patterns.push(`Headache was recorded on ${head.length} days; ${head.filter(d=>d.pain!=null).length} overlapped with a pain record.`);if(flash.length>=2)patterns.push(`Hot flashes were recorded on ${flash.length} days (${pct(flash.length,days.length)}% of this range).`);if(panic.length)patterns.push(`${panic.length} panic-attack day${panic.length===1?"":"s"} recorded.`);if(common!=null)patterns.push(`Most common recorded bowel value: Type ${common}.`);const detailPages=packTimeline([...logged].reverse(),meds,medLog),scheduled=meds.filter(m=>!m.asNeeded&&(m.times?.length??0)>0),prn=meds.filter(m=>m.asNeeded),total=1+detailPages.length+2,refPage=2+detailPages.length,medPage=refPage+1;return <div className="hrDoc">
<section className="pdf-sheet"><Header range={range} page={1}/><div className="meta"><span><b>Generated</b> {longDate(iso(new Date()),locale)}</span><span><b>Logged days</b> {logged.length} of {days.length} ({pct(logged.length,days.length)}%)</span></div><h2>At a glance</h2><div className="metrics six"><Metric label="Pain" value={`${fmt(pAvg)} / 10`} hint={`Min ${fmt(pMin)} · Max ${fmt(pMax)}`}/><Metric label="Headache" value={`${head.length} days`} hint={`Avg ${fmt(hAvg)} · Max ${fmt(hMax)}`}/><Metric label="Hot flashes" value={`${flash.length} days`} hint={`Avg ${fmt(fAvg)} · Max ${fmt(fMax)}`}/><Metric label="Other symptoms" value={`${tet.length+panic.length+nausea.length}`} hint="Tetany · panic · nausea"/><Metric label="Sleep" value={sAvg==null?"—":`${fmt(sAvg)} h`} hint={`${sleep.length} recorded days`}/><Metric label="Bowel" value={common!=null?`Type ${common}`:"—"} hint={`${bowel.length} records`}/></div><div className="overviewGrid"><div><h2>Symptom timeline <small>(intensity heatmap)</small></h2><Heatmap days={days}/><div className="heatLegend"><span><i data-l="0"/>No data</span><span><i data-l="1"/>Mild (1–25%)</span><span><i data-l="2"/>Moderate (26–50%)</span><span><i data-l="3"/>Severe (51–75%)</span><span><i data-l="4"/>Very severe (76–100%)</span><span><i data-period="true"/>Period / spotting</span></div><h2>Pain trend <small>(0–10) · daily average</small></h2><DailyPainTrend days={days} locale={locale}/></div><div><h2>Symptom frequency <small>(days)</small></h2><div className="bars">{symptoms.map(([name,a])=><div key={name}><span>{name}</span><i><b style={{width:`${Math.max(2,pct(a.length,days.length))}%`}}/></i><strong>{a.length} ({pct(a.length,days.length)}%)</strong></div>)}</div><div className="coverage"><b>Data coverage</b><p>{days.length-logged.length} of {days.length} days have no meaningful health log. Absence of a record is not treated as absence of a symptom.</p></div><h2>Observed patterns</h2><div className="patterns">{patterns.map((x,i)=><div key={x}><b>{String(i+1).padStart(2,"0")}</b><span>{x}</span></div>)}</div></div></div><Foot page={1} total={total}/></section>
{detailPages.map((rows,i)=>{const page=2+i;return <section className="pdf-sheet detailSheet" key={page}><Header range={range} page={page}/><h2>Detailed timeline</h2><p className="subnote">Only days with meaningful recorded data are shown. Newest first.</p><table className="detail"><thead><tr><th>Date</th><th>Pain & symptoms</th><th>Taken meds</th><th>Extra meds / PRN</th><th>TENS</th><th>Context / notes</th></tr></thead><tbody>{rows.length?rows.map(d=><tr key={d.key}><td><b>{longDate(d.key,locale)}</b></td><td>{facts(d).join(" · ")||"—"}</td><td>{takenText(d,meds,medLog)}</td><td>{extraText(d)}</td><td>{tensText(d)}</td><td>{contextText(d)}</td></tr>):<tr><td colSpan={6}>No meaningful daily records in this range.</td></tr>}</tbody></table><Foot page={page} total={total}/></section>})}
<section className="pdf-sheet"><Header range={range} page={refPage}/><div className="scaleTop"><PainBarTrend days={days} locale={locale}/><div><h2>Pain scale <small>(0–10)</small></h2><div className="painScale">{PAIN_SCALE.map(([n,t])=><div key={n}><b>{n}</b><span>{t}</span></div>)}</div></div></div><div className="referenceGrid"><div className="referenceBox"><h2>Symptom intensity scale <small>(heatmap)</small></h2><div className="scaleLegend"><div><i data-l="0"/><b>No data</b><span>No entry for this day</span></div><div><i data-l="1"/><b>Mild</b><span>1–25% of maximum intensity</span></div><div><i data-l="2"/><b>Moderate</b><span>26–50%</span></div><div><i data-l="3"/><b>Severe</b><span>51–75%</span></div><div><i data-l="4"/><b>Very severe</b><span>76–100%</span></div><div><i data-period="true"/><b>Period / spotting</b><span>Menstruation or spotting</span></div></div></div><div className="referenceBox"><h2>Bowel type scale <small>(Bristol Stool Chart)</small></h2><div className="bowelLegend">{BOWEL.map(([type,desc])=><div key={type}><b>Type {type}</b><i data-type={type}/><span>{desc}</span></div>)}</div></div></div><Foot page={refPage} total={total}/></section>
<section className="pdf-sheet"><Header range={range} page={medPage}/><h2>Medication & supplement adherence</h2><table className="adherenceTable"><thead><tr><th>Medication / supplement</th><th>Schedule (per day)</th><th>Taken / tracked</th><th>Adherence</th><th>Adherence %</th></tr></thead><tbody>{scheduled.length?scheduled.map(m=>{const a=adherenceForMed(m,days,medLog);return <tr key={m.id}><td><b>{m.name}</b>{m.dose?<small>{m.dose}</small>:null}</td><td>{m.times.length}x</td><td>{a?`${a.taken} / ${a.total}`:"No tracking records"}</td><td>{a?<div className="adhBar"><i><span style={{width:`${a.pct}%`}}/></i></div>:"—"}</td><td>{a?`${a.pct}%`:"—"}</td></tr>}):<tr><td colSpan={5}>No scheduled medication.</td></tr>}</tbody></table>{prn.length?<><h2>As-needed medication</h2><table className="prnTable"><thead><tr><th>Medication</th><th>Recorded uses</th></tr></thead><tbody>{prn.map(m=>{const uses=days.reduce((n,d)=>n+(d.log.extraMeds??[]).filter(x=>x.name.toLowerCase()===m.name.toLowerCase()).length,0);return <tr key={m.id}><td>{m.name}</td><td>{uses}</td></tr>})}</tbody></table></>:null}<p className="adherenceNote">Adherence is calculated from this medication’s own scheduled dose slots (medication ID + time). Legacy medication-level records are used only as a fallback.</p><Foot page={medPage} total={total}/></section>
</div>}

function CalendarPicker({value,target,min,max,locale,onCancel,onApply}:{value:string;target:PickerTarget;min?:string;max?:string;locale:string;onCancel:()=>void;onApply:(v:string)=>void}){const initial=fromIso(value),[month,setMonth]=useState(new Date(initial.getFullYear(),initial.getMonth(),1));const y=month.getFullYear(),m=month.getMonth(),first=new Date(y,m,1),off=(first.getDay()+6)%7,start=new Date(y,m,1-off),cells=Array.from({length:42},(_,i)=>{const d=new Date(start);d.setDate(start.getDate()+i);return d}),title=new Intl.DateTimeFormat(locale,{month:"long",year:"numeric"}).format(month),week=["Mon","Tue","Wed","Thu","Fri","Sat","Sun"],ok=(k:string)=>(!min||k>=min)&&(!max||k<=max);return <div className="calendarOverlay" role="dialog" aria-modal="true" onClick={onCancel}><div className="calendarCard" onClick={e=>e.stopPropagation()}><div className="calendarTop"><div><small>Select date · {target==="from"?"From":"To"}</small><strong>{longDate(value,locale)}</strong></div><button aria-label="Close" onClick={onCancel}>×</button></div><div className="calendarNav"><button onClick={()=>setMonth(new Date(y,m-1,1))}>‹</button><strong>{title}</strong><button onClick={()=>setMonth(new Date(y,m+1,1))}>›</button></div><div className="calendarGrid calendarWeek">{week.map(w=><span key={w}>{w}</span>)}</div><div className="calendarGrid">{cells.map(d=>{const k=iso(d),enabled=ok(k);return <button key={k} disabled={!enabled} data-out={d.getMonth()!==m} data-active={k===value} onClick={()=>{if(enabled)onApply(k)}}>{d.getDate()}</button>})}</div><p className="pickerHint">Tap a date to select it. The picker closes automatically.</p></div></div>}

export function HealthReportPageV2(){const{data,hydrated}=useBixbo();const{t,language}=useI18n();const view=hydrated?data:EMPTY,locale=language==="sk"?"sk-SK":"en-GB",today=iso(new Date());const[preset,setPreset]=useState<Preset>("30"),[customStart,setCustomStart]=useState(add(today,-29)),[customEnd,setCustomEnd]=useState(today),[picker,setPicker]=useState<PickerTarget|null>(null),[preview,setPreview]=useState(false),[busy,setBusy]=useState(false),previewRef=useRef<HTMLDivElement|null>(null);const[start,end]=useMemo(()=>preset==="custom"?[customStart,customEnd]:[add(today,-(Number(preset)-1)),today],[preset,customStart,customEnd,today]);const days=useMemo(()=>Array.from({length:countDays(start,end)},(_,i)=>{const k=add(start,i);return summarize(k,view.dayLogs[k]??{},view.dayNotes?.[k])}),[start,end,view.dayLogs,view.dayNotes]);const range=`${longDate(start,locale)} – ${longDate(end,locale)} · ${days.length}-day report`,report=<Report days={days} meds={view.meds} medLog={view.medLog} range={range} locale={locale}/>;const apply=(v:string)=>{if(picker==="from"){setCustomStart(v);if(v>customEnd)setCustomEnd(v)}else if(picker==="to"){setCustomEnd(v);if(v<customStart)setCustomStart(v)}setPicker(null)};const savePdf=async()=>{if(busy||!previewRef.current)return;setBusy(true);try{const[{default:html2canvas},{jsPDF}]=await Promise.all([import("html2canvas"),import("jspdf")]);const sheets=[...previewRef.current.querySelectorAll<HTMLElement>(".pdf-sheet")],pdf=new jsPDF({orientation:"landscape",unit:"mm",format:"a4",compress:true});for(let i=0;i<sheets.length;i++){if(i)pdf.addPage("a4","landscape");const c=await html2canvas(sheets[i],{scale:2,backgroundColor:"#fff",useCORS:true,logging:false}),pw=pdf.internal.pageSize.getWidth(),ph=pdf.internal.pageSize.getHeight(),r=Math.min(pw/c.width,ph/c.height),w=c.width*r,h=c.height*r;pdf.addImage(c.toDataURL("image/jpeg",.96),"JPEG",(pw-w)/2,(ph-h)/2,w,h,undefined,"FAST")}pdf.save(`BIXBO-Health-Report-${start}-${end}.pdf`)}finally{setBusy(false)}};return <AppShell title={<Link to="/profile" className="flex items-center gap-2"><ArrowLeft className="h-5 w-5"/>{t("PDF reports")}</Link>}><style>{CSS}</style><div className="reportRoot px-4 pb-28 pt-3"><div className="controls"><section className="rounded-3xl bg-surface p-4 shadow-sm ring-1 ring-border/80"><p className="font-serif text-xl font-bold">Health Report</p><p className="mt-1 text-xs text-muted-foreground">Doctor-friendly summary of your recorded health data.</p><div className="presets">{(["7","30","90","365","custom"] as Preset[]).map(x=><button key={x} data-active={preset===x} onClick={()=>setPreset(x)}>{x==="365"?"1 year":x==="custom"?"Custom":`${x} days`}</button>)}</div>{preset==="custom"&&<div className="custom"><button onClick={()=>setPicker("from")}><b>From</b><span>{longDate(customStart,locale)}</span></button><button onClick={()=>setPicker("to")}><b>To</b><span>{longDate(customEnd,locale)}</span></button></div>}<button className="previewBtn" onClick={()=>setPreview(true)}>Preview / Save PDF</button></section></div><div className="screenPreview">{report}</div></div>{picker&&<CalendarPicker target={picker} value={picker==="from"?customStart:customEnd} min={picker==="to"?customStart:undefined} max={picker==="from"?customEnd:undefined} locale={locale} onCancel={()=>setPicker(null)} onApply={apply}/>} {preview&&<div ref={previewRef} className="modal"><div className="toolbar"><button onClick={()=>setPreview(false)}>← Back</button><span>{range}</span><button disabled={busy} onClick={savePdf}>{busy?"Creating PDF…":"Save PDF"}</button></div>{report}</div>}</AppShell>}

const CSS=String.raw`
.reportRoot{--olive:#7f8950;--ink:#20261d;--muted:#707668;--line:#dde1cf;--pale:#f7f8f2;--pink:#f29aa5}.controls{max-width:1120px;margin:0 auto 16px}.presets{display:grid;grid-template-columns:repeat(5,1fr);gap:7px;margin-top:14px}.presets button,.custom button{height:42px;border:1px solid hsl(var(--border));border-radius:14px;background:hsl(var(--surface));font-size:12px;font-weight:700}.presets button[data-active=true]{background:#f0f3e6;border-color:#90995f;color:#596238}.custom{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:12px}.custom button{height:54px;display:flex;justify-content:space-between;align-items:center;padding:0 14px}.previewBtn{margin-top:14px;width:100%;height:44px;border-radius:16px;background:hsl(var(--primary));color:hsl(var(--primary-foreground));font-weight:700}.screenPreview{max-width:1120px;margin:auto;overflow:auto}.hrDoc{display:grid;gap:18px}.pdf-sheet{position:relative;box-sizing:border-box;width:1120px;height:792px;margin:auto;background:#fff;color:var(--ink);padding:28px 40px 42px;font-family:Inter,Arial,sans-serif;box-shadow:0 10px 34px #0001;overflow:hidden}.hrHeader{display:flex;justify-content:space-between;align-items:flex-start;padding-bottom:8px}.hrHeader b{font-size:11px;letter-spacing:.36em}.hrHeader h1{font-family:"Instrument Serif",Georgia,serif;font-size:38px;line-height:1;margin:7px 0 0}.hrHeader h3{font-family:"Instrument Serif",Georgia,serif;font-size:14px;margin:3px 0}.hrHeader>span{font-size:8px}.pdf-sheet h2,.barTitle{font-family:"Instrument Serif",Georgia,serif;font-size:19px;margin:10px 0 6px}.pdf-sheet h2 small,.barTitle small{font:500 8px Inter;color:var(--olive)}.meta{display:flex;justify-content:flex-end;gap:22px;font-size:8px}.metrics{display:grid;gap:8px}.metrics.six{grid-template-columns:repeat(6,1fr)}.metric{border:1px solid var(--line);border-radius:9px;padding:9px 11px;min-height:72px}.metric span{font-size:6.5px;font-weight:700;text-transform:uppercase}.metric strong{display:block;font-family:"Instrument Serif",Georgia,serif;font-size:22px;margin-top:7px}.metric small{display:block;font-size:6.5px;color:var(--muted);margin-top:5px}.overviewGrid{display:grid;grid-template-columns:1.55fr .45fr;gap:26px}.heat{border:1px solid var(--line)}.heatRow{display:grid;grid-template-columns:90px repeat(var(--cells),1fr)}.heatRow span{font-size:6.5px;padding:3px;border-top:1px solid #eef0e7}.heatHead b{font-size:5.8px;text-align:center;padding:2px;color:var(--muted)}.heatRow i{min-height:16px;border-left:1px solid #eef0e7;border-top:1px solid #eef0e7;background:#fff}.heatRow i[data-l="1"],.heatLegend i[data-l="1"],.scaleLegend i[data-l="1"]{background:#e5e8d5}.heatRow i[data-l="2"],.heatLegend i[data-l="2"],.scaleLegend i[data-l="2"]{background:#c8cda5}.heatRow i[data-l="3"],.heatLegend i[data-l="3"],.scaleLegend i[data-l="3"]{background:#a5ad73}.heatRow i[data-l="4"],.heatLegend i[data-l="4"],.scaleLegend i[data-l="4"]{background:#5f6b32}.heatRow i[data-period="true"],.heatLegend i[data-period="true"],.scaleLegend i[data-period="true"]{background:var(--pink)!important}.heatLegend{display:flex;gap:10px;flex-wrap:wrap;margin:6px 0;font-size:6.5px}.heatLegend span{display:flex;align-items:center;gap:4px}.heatLegend i,.scaleLegend i{width:10px;height:10px;border:1px solid var(--line);background:#fff}.chart{width:100%;height:158px;overflow:visible}.chart line{stroke:#e9ecdf}.chart polyline{fill:none;stroke:var(--olive);stroke-width:2}.chart circle{fill:var(--olive)}.chart text{font-size:7px;fill:#4c5445}.chart .pointLabel{font-weight:700}.trendLegend{display:flex;justify-content:space-between;font-size:6.5px;color:var(--muted)}.trendLegend i,.miniLegend>i{width:22px;height:2px;background:var(--olive);display:inline-block}.bars{display:grid;gap:7px}.bars>div{display:grid;grid-template-columns:70px 1fr 44px;gap:6px;align-items:center;font-size:7px}.bars i{height:7px;background:#f0f2e9;border-radius:99px;overflow:hidden}.bars i b{display:block;height:100%;background:#8f9859}.coverage{margin-top:12px;font-size:7px}.coverage p{color:var(--muted)}.patterns{display:grid;gap:7px}.patterns>div{display:grid;grid-template-columns:18px 1fr;gap:6px;font-size:7px}.detail{width:100%;border-collapse:collapse;font-size:7.8px;table-layout:fixed}.detail th{background:#f1f3e9;text-align:left;padding:7px;border:1px solid var(--line)}.detail td{padding:7px;border:1px solid var(--line);vertical-align:top;line-height:1.3;overflow-wrap:anywhere}.detail tbody tr:nth-child(even){background:#fafbf6}.detail th:nth-child(1){width:11%}.detail th:nth-child(2){width:22%}.detail th:nth-child(3){width:17%}.detail th:nth-child(4){width:17%}.detail th:nth-child(5){width:9%}.detail th:nth-child(6){width:24%}.subnote{font-size:7px;color:var(--muted);margin:0 0 7px}.scaleTop{display:grid;grid-template-columns:1fr 190px;gap:28px}.painBars{height:165px;display:grid;grid-template-columns:repeat(var(--bars),1fr);gap:10px;align-items:end;border-bottom:1px solid #dfe3d7;padding:12px 8px 0}.painBar{display:grid;grid-template-rows:1fr auto;height:100%;text-align:center}.painBarArea{position:relative;height:135px;display:flex;align-items:flex-end;justify-content:center}.painBarArea i{display:block;width:min(26px,70%);background:#6f783d}.painBarArea b{position:absolute;font-size:7px;transform:translateY(-2px)}.painBarArea em{font-size:8px;color:#aaa;align-self:end}.painBar span{font-size:7px;margin-top:4px}.miniLegend{font-size:7px;margin-top:8px}.miniLegend .nodata{margin-left:14px;color:#999}.painScale{display:grid;gap:6px;margin-top:14px}.painScale>div{display:grid;grid-template-columns:42px 1fr;font-size:8px}.referenceGrid{display:grid;grid-template-columns:1fr 1fr;gap:26px;margin-top:22px}.referenceBox{border:1px solid var(--line);border-radius:8px;padding:12px 16px;min-height:260px}.scaleLegend{display:grid;gap:12px;margin-top:15px}.scaleLegend>div{display:grid;grid-template-columns:18px 72px 1fr;align-items:center;font-size:8px}.bowelLegend{display:grid;gap:8px;margin-top:12px}.bowelLegend>div{display:grid;grid-template-columns:55px 50px 1fr;align-items:center;font-size:8px}.bowelLegend i{height:12px;position:relative}.bowelLegend i:before{content:"";position:absolute;background:#6f783d;border-radius:50%;width:38px;height:9px;left:2px;top:2px}.adherenceTable,.prnTable{width:100%;border-collapse:collapse;font-size:8.5px;table-layout:fixed}.adherenceTable th,.prnTable th{background:#f1f3e9;padding:9px;border:1px solid var(--line);text-align:left}.adherenceTable td,.prnTable td{padding:11px 9px;border:1px solid var(--line);vertical-align:middle}.adherenceTable td small{display:block;color:var(--muted)}.adhBar i{display:block;height:9px;background:#f0f2e9}.adhBar span{display:block;height:100%;background:#6f783d}.adherenceNote{font-size:7.5px;color:var(--muted);margin-top:16px}.prnTable{width:55%}.empty{height:120px;display:grid;place-items:center;color:var(--muted)}footer{position:absolute;left:40px;right:40px;bottom:18px;border-top:1px solid var(--line);padding-top:6px;display:flex;justify-content:space-between;font-size:6px;color:#8b9084}.modal{position:fixed;inset:0;z-index:10050;overflow:auto;background:#eceee8;padding:72px 14px 28px}.toolbar{position:fixed;z-index:10060;top:max(env(safe-area-inset-top),10px);left:50%;transform:translateX(-50%);width:min(760px,calc(100% - 24px));display:flex;align-items:center;gap:8px;padding:8px;border-radius:16px;background:#fffffff5;box-shadow:0 8px 30px #0002}.toolbar span{flex:1;text-align:center;font-size:9px}.toolbar button{height:38px;border-radius:11px;padding:0 13px;background:#eef1e5;font-size:10px;font-weight:700}.toolbar button:last-child{background:#7f8950;color:#fff}.calendarOverlay{position:fixed;inset:0;z-index:2147483000;background:rgba(20,25,15,.58);display:flex;align-items:flex-end}.calendarCard{width:min(430px,100%);margin:0 auto 84px;background:#fff;border-radius:24px;padding:16px;color:#20261d;box-shadow:0 30px 80px #0007}.calendarTop{display:flex;justify-content:space-between;align-items:flex-start}.calendarTop small{display:block;color:#707668}.calendarTop strong{display:block;font-size:18px;margin-top:3px}.calendarTop button,.calendarNav button{padding:8px 12px;border-radius:12px;background:#f2f3ed;font-weight:700}.calendarNav{display:grid;grid-template-columns:44px 1fr 44px;align-items:center;margin-top:12px}.calendarNav strong{text-align:center}.calendarGrid{display:grid;grid-template-columns:repeat(7,1fr);gap:5px;margin-top:7px}.calendarWeek span{text-align:center;font-size:10px;color:#777}.calendarGrid button{aspect-ratio:1;border-radius:50%;background:#f7f8f2;font-weight:700}.calendarGrid button[data-active=true]{background:#7f8950;color:#fff}.calendarGrid button[data-out=true]{opacity:.35}.calendarGrid button:disabled{opacity:.15}.pickerHint{text-align:center;margin-top:10px;font-size:10px;color:#707668}@media(max-width:700px){.presets{grid-template-columns:repeat(3,1fr)}.custom{grid-template-columns:1fr}.calendarCard{margin-bottom:84px}}@media print{.controls,.toolbar,.calendarOverlay{display:none!important}.screenPreview{display:none!important}.modal{position:static!important;padding:0;background:#fff}.pdf-sheet{box-shadow:none;break-after:page;width:297mm;height:210mm}@page{size:A4 landscape;margin:0}}
`;