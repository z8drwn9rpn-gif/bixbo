name: Temporary i18n audit

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/i18n-audit-temp.yml'

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan hardcoded UI strings
        run: python .github/scripts/i18n_audit.py
