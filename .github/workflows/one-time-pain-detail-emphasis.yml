name: One-time Pain detail emphasis

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  patch:
    if: github.event.head_commit.message == 'Trigger Pain detail emphasis patch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Apply focused UI patch
        run: python3 scripts/patch-pain-detail-emphasis.py
      - name: Verify
        run: |
          git diff --check
          bun install --frozen-lockfile
          bun run typecheck
          bun test src/lib/__tests__/pain-symptom-update-regression.test.ts
      - name: Commit
        run: |
          rm -f scripts/patch-pain-detail-emphasis.py .github/workflows/one-time-pain-detail-emphasis.yml
          git config user.name "BIXBO Fix Bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "Polish Pain detail emphasis and stress scale"
          git push origin HEAD:main
