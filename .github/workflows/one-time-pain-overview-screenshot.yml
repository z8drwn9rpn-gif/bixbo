name: One-time Pain overview screenshot patch

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  patch:
    if: github.event.head_commit.message == 'Trigger Pain overview screenshot patch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Apply focused UI patch
        run: python3 scripts/patch-pain-overview-screenshot.py
      - name: Verify focused change
        run: |
          git diff --check
          bun install --frozen-lockfile
          bun run typecheck
          bun test src/lib/__tests__/pain-symptom-update-regression.test.ts
      - name: Commit focused change
        run: |
          rm -f scripts/patch-pain-overview-screenshot.py .github/workflows/one-time-pain-overview-screenshot.yml
          git config user.name "BIXBO Fix Bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "Match Pain overview to approved screenshot"
          git push origin HEAD:main
