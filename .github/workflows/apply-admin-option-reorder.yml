name: Apply admin option reorder
on:
  push:
    branches: [agent/admin-option-reorder]
    paths:
      - .github/workflows/apply-admin-option-reorder.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/admin-option-reorder
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python .github/scripts/apply_admin_option_reorder.py
      - run: rm .github/scripts/apply_admin_option_reorder.py .github/workflows/apply-admin-option-reorder.yml
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/components/AdminEditOverlay.tsx src/lib/__tests__/admin-option-order.test.ts .github
          git commit -m 'add admin reorder controls for log choices'
          git push origin HEAD:agent/admin-option-reorder
