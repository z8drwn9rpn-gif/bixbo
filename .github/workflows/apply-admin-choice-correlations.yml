name: Apply admin choice correlations
on:
  push:
    branches:
      - agent/admin-correlation-choice-fields
    paths:
      - .github/scripts/apply_admin_choice_correlations.py
      - .github/workflows/apply-admin-choice-correlations.yml
permissions:
  contents: write
jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/admin-correlation-choice-fields
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python .github/scripts/apply_admin_choice_correlations.py
      - run: rm .github/scripts/apply_admin_choice_correlations.py .github/workflows/apply-admin-choice-correlations.yml
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'add admin choice fields to correlations'
          git push origin HEAD:agent/admin-correlation-choice-fields
