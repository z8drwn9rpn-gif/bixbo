name: Apply temp drag
on:
  push:
    branches: [agent/temp-field-drag]
    paths:
      - '.github/scripts/apply_temp_drag.py'
      - '.github/workflows/apply-temp-drag.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
      - run: python .github/scripts/apply_temp_drag.py
      - run: rm .github/scripts/apply_temp_drag.py .github/workflows/apply-temp-drag.yml
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'make Temp Sleep Weight fields truly reorderable'
          git push
