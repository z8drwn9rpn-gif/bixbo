name: Apply admin correlation fields
on:
  push:
    branches:
      - agent/admin-correlation-toggle-fields
    paths:
      - .github/scripts/apply_admin_correlation_fields.py
      - .github/workflows/apply-admin-correlation-fields.yml
permissions:
  contents: write
jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/admin-correlation-toggle-fields
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python .github/scripts/apply_admin_correlation_fields.py
      - run: rm .github/scripts/apply_admin_correlation_fields.py .github/workflows/apply-admin-correlation-fields.yml
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'add admin toggle fields to correlations'
          git push origin HEAD:agent/admin-correlation-toggle-fields
