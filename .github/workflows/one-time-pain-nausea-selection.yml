name: One-time Pain nausea selection fix

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  patch:
    if: github.event.head_commit.message == 'Trigger Pain nausea selection fix'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Apply focused fix
        run: python3 scripts/patch-pain-nausea-selection.py
      - name: Verify focused fix
        run: |
          git diff --check
          bun install --frozen-lockfile
          bun run typecheck
          bun test src/lib/__tests__/pain-symptom-update-regression.test.ts
      - name: Commit focused fix
        run: |
          rm -f scripts/patch-pain-nausea-selection.py .github/workflows/one-time-pain-nausea-selection.yml
          git config user.name "BIXBO Fix Bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "Fix Pain nausea symptom selection persistence"
          git push origin HEAD:main
