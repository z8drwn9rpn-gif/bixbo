name: Inspect custom heatmap
on:
  push:
    paths:
      - '.github/workflows/inspect-custom-heatmap.yml'
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python .github/scripts/inspect-custom-heatmap.py
